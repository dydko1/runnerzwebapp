spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mirek
    username: mirek
    password: Mirdyd12345^
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create-drop
#      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    databases: postgresql
    databases-platform: org.hibernate.dialect.PostgreSQLDialect

  output:
    ansi:
      enabled: ALWAYS